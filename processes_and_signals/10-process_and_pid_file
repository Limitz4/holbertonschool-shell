#!/usr/bin/env bash
# Process with PID file and signal handling

# Create PID file
echo $$ > /var/run/myscript.pid

# Signal handlers - SIGTERM və SIGQUIT üçün eyni handler
trap 'echo "I hate the kill command"; rm -f /var/run/myscript.pid; exit' SIGTERM
trap 'rm -f /var/run/myscript.pid; exit' SIGQUIT
trap 'echo "Y U no love me?!"' SIGINT

# Main loop
while true
do
    echo "To infinity and beyond"
    sleep 2
done
